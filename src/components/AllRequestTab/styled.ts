import styled from "styled-components/native";
import { SPACING } from "../../constants";

const AllRequestTabMainContainer = styled.View`
  // padding-bottom: ${SPACING.h60}px
`;

const AllRequestTabInnerContainer = styled.View`

`;

export {AllRequestTabMainContainer, AllRequestTabInnerContainer}