import React from 'react';
import {SafeAreaView} from 'react-native';
import {mainBackgroundColor} from '../../constants';

const BarStatus = () => {
  return (
    <SafeAreaView
      style={{flex: 0, backgroundColor: mainBackgroundColor}}></SafeAreaView>
  );
};

export default BarStatus;
